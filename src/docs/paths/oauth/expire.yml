summary: "Expire authentication token"
operationId: notImplemented # expireOauth
tags: ["oauth"]
responses:
  200:
    description: "Token successfully expired"
  500:
    description: "Problem expiring token"
