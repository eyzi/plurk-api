summary: "Get fans of current user"
operationId: getOwnFans
tags: ["me"]
parameters:
  - name: offset
    in: query
    description: Offset of list. (defaults to 0)
    schema:
      type: number
  - name: limit
    in: query
    description: Limit of list. (defaults to 1000)
    schema:
      type: number
responses:
  $ref: "../../common/lazy-passthrough-responses.yml"