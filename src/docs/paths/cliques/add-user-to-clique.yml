summary: Add user to clique
operationId: addCliqueUser
tags:
  - "cliques"
parameters:
  - name: name
    in: path
    description: Clique name to add user
    required: true
    schema:
      type: string
  - name: userId
    in: path
    description: User to add to clique
    required: true
    schema:
      type: string
responses:
  $ref: "../../common/lazy-passthrough-responses.yml"