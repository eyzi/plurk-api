summary: Rename clique
operationId: renameClique
tags:
  - "cliques"
parameters:
  - name: name
    in: path
    description: Clique to rename
    required: true
    schema:
      type: string
  - name: name
    in: query
    description: New name for the clique
    required: true
    schema:
      type: string
responses:
  $ref: "../../common/lazy-passthrough-responses.yml"